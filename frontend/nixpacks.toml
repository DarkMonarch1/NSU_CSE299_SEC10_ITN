[phases.setup]
nixPkgs = ["nodejs"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "npm start"

[variables]
PORT = "3000"
NODE_ENV = "production"
NEXT_PUBLIC_API_URL = "https://careersetu.up.railway.app"