[phases.build]
nixPkgs = ["nodejs-22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmd = "npm start"

[variables]
PORT = "3000"
NODE_ENV = "production"
NEXT_PUBLIC_API_URL = "https://careersetu.up.railway.app"