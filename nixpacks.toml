[phases.build]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[variables]
PORT = "8080"
PYTHONUNBUFFERED = "1"
